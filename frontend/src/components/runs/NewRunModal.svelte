<script lang="ts">
  // Stub — T6 replaces this with the full implementation.
  import type { Course, Version } from '../../lib/types';
  let { course: _course, versions: _versions, onClose: _onClose }: {
    course: Course;
    versions: Version[];
    onClose: () => void;
  } = $props();
</script>
<!-- placeholder -->
